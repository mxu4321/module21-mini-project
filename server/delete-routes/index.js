// ❄️ the routes folder is not needed when using the React Router

// const router = require('express').Router();
// const apiRoutes = require('./api');

// router.use('/api', apiRoutes);

// module.exports = router;
